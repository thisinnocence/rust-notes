# 03 - Result 与错误处理

运行：

```bash
cargo run --bin 03_result
```

核心点：

- Rust 不用异常做常规错误控制流，而是 `Result<T, E>`。
- `?` 用于“出错就早返回”，让错误传播非常轻量。
- `match` 可精确处理成功与失败路径。

对照 C/C++：

- 类似“返回值 + error code”，但类型系统强制你处理错误。
- 不会像忽略返回码那样默默丢错。

工程建议（后续会加）：

- 应用层可用 `anyhow` 聚合上下文。
- 库层可用 `thiserror` 定义精确错误类型。
