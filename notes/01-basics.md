# 01 - 基础语法（对照 C/C++）

运行：

```bash
cargo run --bin 01_basics
```

对照点：
- `let` 默认不可变，`let mut` 才可变。
- `if` 是表达式，可直接返回值给变量。
- `match` 是穷尽匹配，常用于替代层层 `if/else`。
- 字符串字面量 `"..."` 在这里是 `&'static str`，不是 `std::string`。
- 函数返回类型写在 `->` 后。

和 C/C++ 最大差异（当前阶段）：
- 默认不可变，减少“随手改状态”。
- 语句和表达式界限更清晰，组合性更好。
